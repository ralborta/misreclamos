# Railway configuration
# Este archivo permite configurar el comportamiento de Railway

[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Ejecutar migraciones antes del start (opcional)
# Railway puede ejecutar esto si configuras un servicio separado
